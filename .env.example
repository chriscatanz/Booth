# ════════════════════════════════════════════════════════════════════════════
# Trade Show Manager - Environment Variables
# ════════════════════════════════════════════════════════════════════════════
# Copy this file to .env.local and fill in your values

# ─── Supabase ────────────────────────────────────────────────────────────────
# Get these from your Supabase project settings
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Email verification is enabled by default in Supabase.
# Users must verify their email before accessing the platform.

# ─── App URL ─────────────────────────────────────────────────────────────────
# Used for invitation links. Set to your production domain.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ─── Email Provider ──────────────────────────────────────────────────────────
# Options: console (dev), resend, sendgrid
# "console" logs emails to terminal instead of sending them
NEXT_PUBLIC_EMAIL_PROVIDER=console

# Email "from" address (used by Resend/SendGrid)
EMAIL_FROM=Trade Show Manager <noreply@yourdomain.com>

# ─── Resend (if using) ───────────────────────────────────────────────────────
# Get your API key from https://resend.com/api-keys
# RESEND_API_KEY=re_xxxxxxxxxxxxx

# ─── SendGrid (if using) ─────────────────────────────────────────────────────
# Get your API key from https://app.sendgrid.com/settings/api_keys
# SENDGRID_API_KEY=SG.xxxxxxxxxxxxx

# ─── Error Monitoring (Sentry) ───────────────────────────────────────────────
# Create a project at https://sentry.io and get your DSN
# Only active in production
# NEXT_PUBLIC_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# ─── Stripe (Subscriptions) ──────────────────────────────────────────────────
# Get keys from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxx
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxx

# Supabase service role key (for server-side operations)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Create products/prices in Stripe Dashboard and paste IDs here
STRIPE_STARTER_PRICE_ID=price_xxxxxxxxxxxxx
STRIPE_PRO_PRICE_ID=price_xxxxxxxxxxxxx

# Optional: public key for Stripe.js (if needed for custom checkout)
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxx

# ─── Shippo (Package Tracking) ───────────────────────────────────────────────
# Get your API key from https://apps.goshippo.com/settings/api
# Use test key for development, live key for production
SHIPPO_API_KEY=shippo_test_xxxxxxxxxxxxx
